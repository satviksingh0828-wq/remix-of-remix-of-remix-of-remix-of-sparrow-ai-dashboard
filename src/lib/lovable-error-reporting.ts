// Stub — Lovable-specific reporting removed. Errors are logged to the console only.
export function reportLovableError(_error: unknown, _context: Record<string, unknown> = {}) {
  // no-op: was previously forwarded to Lovable's editor telemetry
}
