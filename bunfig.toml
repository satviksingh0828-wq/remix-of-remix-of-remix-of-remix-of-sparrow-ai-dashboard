[install]
saveTextLockfile = true
# minimumReleaseAge is disabled: Vercel's build sandbox blocks the extra npm
# registry calls Bun makes to verify package publish dates, causing
# ConnectionRefused / FailedToOpenSocket errors that fail the entire install.
minimumReleaseAge = 0
